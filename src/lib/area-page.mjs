export function areaMeta(slug,name,postcodes,description){return {path:`/areas/${slug}/`,title:`Bathroom Renovation ${name} | Kensington Bathrooms`,description,schemaType:'WebPage',breadcrumbs:[{name:'Home',path:'/'},{name:'Areas',path:'/areas/'},{name,path:`/areas/${slug}/`}]}}
export function areaRender({name,postcodes,kicker,headline,lede,angles,protection,close}){return `
<section class="page-hero"><div class="page-hero__inner"><p class="area-hero-note">${postcodes}</p><p class="eyebrow">Bathroom renovation in ${name}</p><h1>${headline}</h1><p class="page-hero__lede">${lede}</p></div></section>
<section class="content-band content-band--white"><div class="section-shell"><div class="content-grid"><div><p class="eyebrow">${kicker}</p><h2>What changes with the property.</h2></div><div class="content-copy">${angles.map(([h,p])=>`<h3>${h}</h3><p>${p}</p>`).join('')}<a class="arrow-link" href="/contact/">Arrange a home consultation in ${name}</a></div></div></div></section>
<section class="content-band content-band--stone"><div class="section-shell"><div class="content-grid"><div><p class="eyebrow">Protecting the home</p><h2>The route matters as much as the room.</h2></div><div class="content-copy"><p>${protection}</p><a class="arrow-link" href="/our-approach/">Read our property-protection standard</a></div></div></div></section>
<section class="content-band content-band--white"><div class="section-shell"><div class="content-grid"><div><p class="eyebrow">Genuinely close</p><h2>${name}, without pretending we have a branch there.</h2></div><div class="content-copy"><p>${close}</p><p>Kensington Bathrooms operates from 33 Gunterstone Road, West Kensington W14. The website says where we are based and where we work — those are not presented as the same thing.</p></div></div></div></section>`}
