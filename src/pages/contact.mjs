import {consultationForm} from '../lib/layout.mjs';
export const meta={path:'/contact/',title:'Arrange a Bathroom Consultation | Kensington Bathrooms',description:'Talk through your bathroom project with Nicholas at your home or by appointment at our West Kensington base.',schemaType:'ContactPage',breadcrumbs:[{name:'Home',path:'/'},{name:'Contact',path:'/contact/'}]};
export function render(cfg){
  const form=consultationForm(cfg,'contact');
  const base=cfg.localBase;
  return `<section class="page-hero page-hero--compact"><div class="page-hero__inner"><p class="eyebrow">Arrange a consultation</p><h1>Let's talk through your bathroom.</h1><p class="page-hero__lede">Home consultations, or design meetings by appointment at our base in West Kensington.</p></div></section>
<section class="content-band content-band--deep"><div class="section-shell"><div class="consultation-split consultation-split--contact" style="padding-block:0"><div class="consultation-split__copy"><p class="eyebrow">Our local base</p><h2>Home visit or design meeting.</h2><p>Our service area covers Kensington, West Kensington, Holland Park, South Kensington and Chelsea. We can meet at your home, or arrange a design meeting by appointment at our West Kensington base.</p>${base ? `<address class="local-base-address"><strong>Kensington Bathrooms local base</strong><br>${base.streetAddress}<br>${base.city} ${base.postcode}</address>` : ''}<p class="appointment-note">Appointments only. This is not a walk-in showroom.</p></div>${form ? `<div class="consultation-split__form">${form}</div>` : ''}</div></div></section>`;
}
